//Fibonacci using recursion

int fib(int a) {
    if (a <= 1)
        return a;
    return fib(a-1) + fib(a-2);
}

int main() {
    int ret = 0;

    int a = 9;
    printInt( fib(a) );

    printInt(ret);
    return ret;
}
