//simple function

struct Person {
    string name = "Andrzej";
    int age = 10;
}

Person func() {
    Person person;
    return person;
}

int main() {
    int ret = 0;

    Person person = func();
    printStr( person.name );

    printInt(ret);
    return 0;
}
