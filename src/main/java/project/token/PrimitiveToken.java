package project.token;

public class PrimitiveToken extends Token {

    public PrimitiveToken(TokenType type, int position, int lineNr, int positionAtLine) {
        super(type, position, lineNr, positionAtLine);
    }
}
